Django==1.5.1

# For migrations
South==0.8.4

# For unit testing
mock==1.0.1

# For openid authentication
python-openid==2.2.5

######
# NOTE: issue with mysql in django-openid-auth v0.5, but
# it was fixed in a PR on github, so using that instead.
# https://github.com/jplana/django-openid-auth/issues/1
######
#django-openid-auth==0.5
git+https://github.com/jplana/django-openid-auth@mysql-support

# For reading/writing excel files
xlrd==0.9.2
xlwt==0.7.5

# For debugging (dev only)
django-debug-toolbar==0.11

# For image manipulation
Pillow==2.2.2
matplotlib==1.4.3

# For audio manipulation
mutagen==1.25.1

# For django mixins
django-braces>=1.3

# For LTI
django-cached-authentication-middleware>=0.2.0
git+https://github.com/Harvard-ATG/django-auth-lti@master#egg=django-auth-lti
