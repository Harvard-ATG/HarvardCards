This directory is organized as follows:

├── components/ Packages of collaborating objects/classes.
├── lib/        Third-party packages and libraries (i.e. jQuery, etc)
├── mixins/     Mixins or groups of methods that can be "mixed" into any class/object.
├── models/     Domain or business objects. 
├── modules/    Controller objects that manage a section of the DOM.
├── spec/       Unit tests (Jasmine framework).
├── utils/      General purpose utilities.
└── views/      Display-oriented objects that are responsible for view logic.

All libraries are intended to be used with AMD (requirejs). 
